/**
 * Route file
 * File định tuyến
 *
 * Purpose:
 * - Declare the Express router for this module.
 * - Map HTTP methods and paths to controller functions.
 * - Attach request validators and route-level middlewares when needed.
 *
 * Mục đích:
 * - Khai báo Express router cho module này.
 * - Ánh xạ HTTP method và path tới controller tương ứng.
 * - Gắn validator và middleware ở cấp route khi cần.
 *
 * Suggested structure:
 * 1. Import Router from Express.
 * 2. Import controller functions from the controller file.
 * 3. Import validators from the validator file.
 * 4. Create `const router = Router()`.
 * 5. Define routes such as `router.get(...)`, `router.post(...)`.
 * 6. Export the router as default.
 *
 * Cấu trúc gợi ý:
 * 1. Import Router từ Express.
 * 2. Import các hàm controller từ file controller.
 * 3. Import validator từ file validator.
 * 4. Tạo `const router = Router()`.
 * 5. Khai báo route như `router.get(...)`, `router.post(...)`.
 * 6. Export router mặc định.
 */
