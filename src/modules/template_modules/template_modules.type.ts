/**
 * Type file
 * File định nghĩa kiểu dữ liệu
 *
 * Purpose:
 * - Define TypeScript types and interfaces used inside this module.
 * - Keep request params, request bodies, responses, and service DTOs reusable.
 *
 * Mục đích:
 * - Định nghĩa TypeScript type và interface dùng trong module này.
 * - Tái sử dụng kiểu dữ liệu cho params, body, response và DTO của service.
 *
 * Suggested structure:
 * 1. Export request body types.
 * 2. Export query/params types.
 * 3. Export response types.
 * 4. Export service input/output DTO types.
 *
 * Cấu trúc gợi ý:
 * 1. Export type cho request body.
 * 2. Export type cho query/params.
 * 3. Export type cho response.
 * 4. Export type DTO input/output của service.
 */
