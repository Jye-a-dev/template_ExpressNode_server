/**
 * Controller file
 * File controller
 *
 * Purpose:
 * - Receive validated requests from routes.
 * - Call service functions.
 * - Format HTTP responses.
 *
 * Mục đích:
 * - Nhận request đã được validate từ route.
 * - Gọi các hàm service.
 * - Định dạng HTTP response trả về client.
 *
 * Suggested structure:
 * 1. Import Request/Response types from Express when needed.
 * 2. Import service functions from the service file.
 * 3. Export controller functions.
 * 4. Keep business logic out of controllers.
 *
 * Cấu trúc gợi ý:
 * 1. Import type Request/Response từ Express khi cần.
 * 2. Import các hàm service từ file service.
 * 3. Export các hàm controller.
 * 4. Không đặt business logic trong controller.
 */
