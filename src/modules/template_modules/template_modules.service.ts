/**
 * Service file
 * File service
 *
 * Purpose:
 * - Hold business logic for this module.
 * - Coordinate models, repositories, external APIs, and domain rules.
 *
 * Mục đích:
 * - Chứa business logic của module này.
 * - Điều phối model, repository, API bên ngoài và các quy tắc nghiệp vụ.
 *
 * Suggested structure:
 * 1. Import needed types from the type file.
 * 2. Import model/database helpers from the model file.
 * 3. Export service functions.
 * 4. Return plain data for controllers to send as HTTP responses.
 *
 * Cấu trúc gợi ý:
 * 1. Import type cần thiết từ file type.
 * 2. Import model/database helper từ file model.
 * 3. Export các hàm service.
 * 4. Trả về dữ liệu thuần để controller gửi HTTP response.
 */
