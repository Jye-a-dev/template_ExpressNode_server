/**
 * Model file
 * File model
 *
 * Purpose:
 * - Define database-facing shapes for this module.
 * - Keep table names, SQL helpers, ORM models, or persistence mappings here.
 *
 * Mục đích:
 * - Định nghĩa cấu trúc dữ liệu làm việc với database cho module này.
 * - Lưu table name, SQL helper, ORM model hoặc mapping tầng lưu trữ tại đây.
 *
 * Suggested structure:
 * 1. Define database entity types.
 * 2. Define table/collection constants.
 * 3. Add ORM model definitions or query helpers if the project uses one.
 * 4. Keep HTTP request/response concerns out of models.
 *
 * Cấu trúc gợi ý:
 * 1. Định nghĩa type entity tương ứng database.
 * 2. Định nghĩa hằng số table/collection.
 * 3. Thêm ORM model hoặc query helper nếu dự án sử dụng.
 * 4. Không đặt logic request/response HTTP trong model.
 */
