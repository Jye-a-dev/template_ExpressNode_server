/**
 * Validator file
 * File validator
 *
 * Purpose:
 * - Define validation schemas for route params, query, and body.
 * - Keep input validation close to the module that owns the API.
 *
 * Mục đích:
 * - Định nghĩa schema validate cho params, query và body của route.
 * - Đặt logic validate input gần module sở hữu API đó.
 *
 * Suggested structure:
 * 1. Import zod or the validation library used by the project.
 * 2. Export body schemas.
 * 3. Export params schemas.
 * 4. Export query schemas.
 * 5. Infer TypeScript types from schemas when useful.
 *
 * Cấu trúc gợi ý:
 * 1. Import zod hoặc thư viện validate mà dự án dùng.
 * 2. Export schema cho body.
 * 3. Export schema cho params.
 * 4. Export schema cho query.
 * 5. Suy luận TypeScript type từ schema khi hữu ích.
 */
